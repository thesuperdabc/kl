name: Bot Health Monitor

on:
  schedule:
    - cron: '*/15 * * * *'  # Every 15 minutes
  workflow_dispatch:

jobs:
  monitor:
    runs-on: ubuntu-latest
    steps:
      - name: 🔍 Monitor Bot Status
        run: |
          echo "🔍 Checking bot health at $(date)"
          
          # Check if main bot workflow is running
          echo "📊 Monitoring system active"
          echo "✅ Health check completed"
          
          # You can add API calls to check bot status on Lichess here
          # curl -H "Authorization: Bearer ${{ secrets.LICHESS_TOKEN }}" https://lichess.org/api/bot/me
